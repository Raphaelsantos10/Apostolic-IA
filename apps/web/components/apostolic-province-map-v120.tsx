"use client";
import{useCallback,useEffect,useMemo,useState}from"react";import{createClient}from"../lib/supabase/client";import styles from"./apostolic-province-map-v120.module.css";
type Slot={slot:number;x:number;y:number;terrain:string;city_id:string|null;city_name:string|null;city_level:number|null;mine:boolean;alliance_tag:string|null};type Route={id:string;from_slot:number;to_slot:number;resource:string;arrives_at:string;mine:boolean};type MapData={province:{name:string;cities:number;capacity:number};region:{name:string;biome:string};capital:{level:number;alliance_id:string|null;alliance_name:string|null;alliance_tag:string|null;progress:number;next_level_points:number};my_alliance:{id:string|null;role:string|null;can_establish:boolean};slots:Slot[];routes:Route[]};
const icons:Record<string,string>={plain:"🏘️",river:"🌊",hill:"⛰️",forest:"🌲"};
export function ApostolicProvinceMapV120(){const[data,setData]=useState<MapData|null>(null),[message,setMessage]=useState(""),[resource,setResource]=useState("stone"),[quantity,setQuantity]=useState(100);const load=useCallback(async()=>{const{data:next}=await createClient().rpc("apostolic_get_province_map");setData(next as MapData);},[]);useEffect(()=>{void load();},[load]);const bySlot=useMemo(()=>new Map(data?.slots.map(s=>[s.slot,s])??[]),[data]);const establish=async()=>{const{error}=await createClient().rpc("apostolic_establish_province_capital");setMessage(error?.message??"Capital da Aliança estabelecida.");void load();};const contribute=async()=>{const{error}=await createClient().rpc("apostolic_contribute_province_capital",{p_resource:resource,p_quantity:quantity,p_idempotency_key:crypto.randomUUID()});setMessage(error?.message??"Contribuição recebida pela capital.");void load();};if(!data)return <section className={styles.shell}>Preparando o mapa provincial…</section>;return <section className={styles.shell}><header><div><small>V116–V120 · {data.region.name}</small><h1>{data.province.name}</h1><p>{data.region.biome} · {data.province.cities}/{data.province.capacity} cidades</p></div>{data.my_alliance.can_establish&&<button onClick={()=>void establish()}>ESTABELECER CAPITAL</button>}</header>{message&&<p className={styles.notice}role="status">{message}</p>}<div className={styles.map}><svg className={styles.routes}viewBox="0 0 100 100"preserveAspectRatio="none">{data.routes.map(r=>{const a=bySlot.get(r.from_slot),b=bySlot.get(r.to_slot);return a&&b?<line key={r.id}x1={a.x}y1={a.y}x2={b.x}y2={b.y}data-mine={r.mine}/>:null})}</svg><div className={styles.capitalNode}><span>🏰</span><b>{data.capital.alliance_tag??"CAPITAL"}</b><small>Nível {data.capital.level}</small></div>{data.slots.map(s=><div key={s.slot}className={styles.slot}style={{left:`${s.x}%`,top:`${s.y}%`}}data-empty={!s.city_id}data-mine={s.mine}><span>{icons[s.terrain]??"🏘️"}</span><b>{s.city_name??`Lote ${s.slot}`}</b>{s.city_id&&<small>Nv. {s.city_level}{s.alliance_tag?` · ${s.alliance_tag}`:""}</small>}</div>)}</div><section className={styles.capital}><div><small>CAPITAL PROVINCIAL COMPARTILHADA</small><h2>{data.capital.alliance_name??"Ainda não estabelecida"}</h2>{data.capital.alliance_id&&<><progress value={data.capital.progress}max={data.capital.next_level_points}/><p>{data.capital.progress}/{data.capital.next_level_points} pontos para o próximo nível</p></>}</div>{data.capital.alliance_id===data.my_alliance.id&&<div className={styles.actions}><select value={resource}onChange={e=>setResource(e.target.value)}><option value="stone">Pedra</option><option value="cedar">Cedro</option><option value="wheat">Trigo</option><option value="oil">Azeite</option><option value="gold">Ouro</option></select><input aria-label="Quantidade"type="number"min={10}max={10000}value={quantity}onChange={e=>setQuantity(Number(e.target.value))}/><button onClick={()=>void contribute()}>CONTRIBUIR</button></div>}</section></section>}
