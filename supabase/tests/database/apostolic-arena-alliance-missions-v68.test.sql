begin;select plan(5);select has_table('public','arena_alliance_mission_checkins');select has_table('public','arena_alliance_mission_claims');select function_returns('public','arena_get_alliance_mission_board',array[]::text[],'jsonb');select function_returns('public','arena_checkin_alliance_mission',array['text'],'jsonb');select function_returns('public','arena_claim_alliance_mission',array['uuid'],'jsonb');select*from finish();rollback;
